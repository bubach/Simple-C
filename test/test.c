#include <stdio.h>

main(argc, argv)
  int argc, argv[];
{
   int a, b, c, d;
   char e, f, g, h;
   char i[100];
   int j[200];

   a = b = c = 'a';

   if(a == 'a') {
     printf("A = 1\n");
   }
}
